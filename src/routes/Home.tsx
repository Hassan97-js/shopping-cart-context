type Props = Record<string, never>;

const Home: React.FC<Props> = () => {
  return (
    <main className='main mt-5'>
      <h1 className='h1 fw-bold'>Home</h1>
    </main>
  );
};

export default Home;
