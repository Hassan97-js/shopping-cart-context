type Props = Record<string, never>;

const About: React.FC<Props> = () => {
  return (
    <main className='main mt-5'>
      <h1 className='h1 fw-bold'>About</h1>
    </main>
  );
};

export default About;
